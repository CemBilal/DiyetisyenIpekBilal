@model IEnumerable<DiyetisyenIpekBilal.Models.Instructions>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.InstructionSen)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.recipe)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.InstructionSen)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.recipe.Name)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.InstructionID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.InstructionID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.InstructionID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
